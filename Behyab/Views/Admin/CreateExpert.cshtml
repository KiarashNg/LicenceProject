@model Behyab.Models.Experty
@{
    ViewBag.Title = "افزودن تخصص";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2 style="font-family:'B Mitra'">
    افزودن تخصص
</h2>

<div style="color:red;">
    @ViewBag.same
</div>

@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.DisplayNameFor(x => x.Name)
        @Html.TextBoxFor(x => x.Name, null, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.Name)
    </div>

    <div class="form-group">
        <input type="submit" value="ذخیره" class="btn btn-primary" />
        @Html.ActionLink("بازگشت به لیست تخصص ها", "Experts", new { name = "" }, new { @class = "btn btn-info" })
    </div>
}

